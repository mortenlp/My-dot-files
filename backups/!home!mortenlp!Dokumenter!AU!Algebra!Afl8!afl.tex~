%Dokumentklassen sættes til memoir
\documentclass[a4paper,11pt,article,oneside]{memoir}

%Sproget sættes til dansk og orddeling indlæses
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[danish]{babel}

%Matematiske symboler og fede tegn i ligninger
\usepackage{amsmath, amssymb, bm, mathtools}

%Tabeller og søjler
\usepackage{array, booktabs, dcolumn}
\newcolumntype{d}[1]{D{,}{,}{#1}}%Justering under komma

%Figurer
\usepackage{graphicx, caption, subfig}
\captionsetup{font=small,labelfont=bf}

\newcommand{\intRing}{\mathbb{Z}[\sqrt{2}]}

\author{Morten Leander Petersen}
\date{\today}
\title{Aflevering, 3.15}

\begin{document}

\maketitle

\paragraph{(i)}
\label{sec:i}

For at vise det ønskede skal vi vise 3 ting:
\begin{enumerate}
\item $\big( \intRing, + \big)$ er en undergruppe af $\big(
  \mathbb{R}, + \big)$
\item $1 \in \intRing$
\item $x,y\in \intRing \Rightarrow xy \in \intRing$
\end{enumerate}
Mht. punkt 1 indses først at
\begin{align*}
  \intRing &\subseteq \mathbb{R} \\
  \intertext{og}
  0 = 0 + 0 &\sqrt{2} \in \intRing
\end{align*}
For givet $x= a + b \sqrt{2} \in \intRing$ betragtes nu $y= -a
-b\sqrt{2} \in \intRing$. Da fås
\begin{equation*}
  x + y = a + b\sqrt{2} - a - b\sqrt{2} = 0
\end{equation*}
Dermed følger at for et vilkårligt element i $\intRing$, ligger også
dens inverse i $\intRing$. Betragt nu
\begin{align}
  x&= a + b\sqrt{2} \in \intRing \label{eq:1}\\
  \intertext{og}
  y&= c + d\sqrt{2} \in \intRing \label{eq:2}
\end{align}
Vi viser at $x + y \in \intRing$.
\begin{equation*}
  x + y= a + b\sqrt{2} + c + d\sqrt{2} = (a + c) + (b + d)\sqrt{2} \in
  \intRing
\end{equation*}
hvor det benyttedes at
\begin{equation*}
  a,b,c,d \in \mathbb{Z} \Rightarrow (a+c),(b+d) \in \mathbb{Z}
\end{equation*}
Vi har nu vist at $\big( \intRing, + \big)$ er en undergruppe af
$\big( \mathbb{R}, + \big)$.

Punkt 2 er oplagt da
\begin{equation*}
  1 = 1 + 0\sqrt{2} \in \intRing
\end{equation*}
så vi betragter nu punkt 3. Lad $x,y\in \intRing$ være givne som i
\eqref{eq:1} og \eqref{eq:2}. Vi viser nu at $xy \in \intRing$.
\begin{align*}
  xy &= \big( a + b\sqrt{2} \big)\big( c + d\sqrt{2} \big) \\
  &= ac + ad\sqrt{2} + cb\sqrt{2} + 2bd \\
  &= (ac + 2bd) + (ad + cb)\sqrt{2} \in \intRing
\end{align*}
hvor det benyttedes at
\begin{equation*}
  a,b,c,d \in \intRing \Rightarrow (ac + 2bd),(ad + cb) \in \intRing
\end{equation*}
Vi slutter hermed at $\intRing$ er en delring af $\mathbb{R}$.

\paragraph{(ii)}
\label{sec:ii}

$\intRing^*$ er mængden af enheder, dvs
\begin{equation*}
  \intRing^* = \big\{ x \in \intRing \big\vert \; \exists y \in \intRing : xy=yx=1 + 0 \sqrt{2} \big\}
\end{equation*}
Betragt nu $1 + \sqrt{2} \in \intRing$. Dette element er en enhed da
\begin{equation*}
  (1 + \sqrt{2}) (-1 + \sqrt{2})= -1 + \sqrt{2} - \sqrt{2} + 2 = 1 + 0\sqrt{2}
\end{equation*}
Jvf. bemærkningen midt på s. 112 er $\big( \intRing^*, \cdot \big)$ en
abelsk gruppe, hvilket specielt betyder at den er lukket under
multiplikation. Vi ved derfor at
\begin{equation*}
  (1 + \sqrt{2})^n \in \intRing^*, \; \forall n\in \mathbb{N}
\end{equation*}
Hvis vi nu kan vise vise at
\begin{equation*}
  (1 + \sqrt{2})^n \neq (1 + \sqrt{2})^m, \; n \neq m
\end{equation*}
så er der mindst lige så mange elementer i $\intRing^*$ som der er
naturlige tal altså derfor uendeligt mange. Det følger dog umiddelbart at
\begin{equation*}
  1 + \sqrt{2} > 1 \Rightarrow (1 + \sqrt{2})^n < (1 + \sqrt{2})^m, \; n < m
\end{equation*}
så det ønskede er dermed vist. 
\end{document}
